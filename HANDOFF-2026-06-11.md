# Codex Proxy Hybrid Control Center - Handoff

Canonical handoff file:

```text
C:\Users\RYZEN\AppData\Local\Temp\codex-proxy-handoff-2026-06-11.md
```

This workspace copy exists so the project folder keeps an easy-to-find pointer.

## Current Status

- Proxy is working on `http://127.0.0.1:1432`.
- Admin dashboard is at `http://127.0.0.1:1432/admin`.
- Claude Code CLI has been verified through the proxy.
- Latest test run: `14/14` passing.

## Most Important Note

Use this for Claude Code:

```text
ANTHROPIC_BASE_URL=http://127.0.0.1:1432
```

Do not add `/v1` for Claude Code. Claude SDK appends `/v1/messages` itself.

Use this for OpenAI-compatible clients:

```text
http://127.0.0.1:1432/v1
```

See the canonical temp handoff above for the complete changelog, backup list, troubleshooting notes, and suggested next work.
